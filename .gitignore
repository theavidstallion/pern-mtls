# --- Dependencies ---
node_modules/
*/node_modules/

# --- Environment Variables (NEVER COMMIT THESE) ---
.env
.env.local
.env.*.local
client/.env
server/.env

# --- Secrets & Certificates (CRITICAL SECURITY) ---
# We ignore the entire certs folder so you don't accidentally publish private keys
certs_final/
certs/
client-secret

# Also ignore any stray certificate files anywhere in the project
*.key
*.pem
*.p12
*.crt
*.csr

# --- Build Outputs ---
dist/
build/
client/dist/

# --- Logs & Debugging ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- System Files ---
.DS_Store
Thumbs.db

# --- IDE Settings ---
.vscode/
.idea/

# --- Docker ---
# Ignore override files used for local testing, but keep the main docker-compose.yml
docker-compose.override.yml